version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - npm ci --include=dev
      - npm run build
      - npm prune --omit=dev
run:
  command: node dist/index.js
  network:
    port: 8080
